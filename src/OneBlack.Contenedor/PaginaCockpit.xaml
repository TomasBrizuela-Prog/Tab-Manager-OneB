<UserControl x:Class="OneBlack.Contenedor.PaginaCockpit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource CodeBg}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Cockpit" Foreground="{StaticResource Fg}"
                       FontSize="20" FontFamily="Consolas" HorizontalAlignment="Center"/>
            <TextBlock Text="Consola y proyectos — próximamente"
                       Foreground="{StaticResource Dim}" FontFamily="Consolas"
                       FontSize="13" Margin="0,8,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>